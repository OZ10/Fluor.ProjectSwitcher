<UserControl x:Class="Fluor.ProjectSwitcher.View.V_Tiles"
             xmlns:local="clr-namespace:Fluor.ProjectSwitcher"    
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding Projects, Mode=OneWay, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ItemsControl Name="icProjects" ItemsSource="{Binding ActiveTileCollection}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        <Button Name="btnAddNew" Grid.Row="1" Content="{DynamicResource PlusIcon}" BorderBrush="#33000000" BorderThickness="1,0,0,1" Background="White" Height="50" Visibility="Visible" Click="btnAddNew_Click" Padding="15" Foreground="{DynamicResource TextBlack54Brush}"/>
    </Grid>
</UserControl>
