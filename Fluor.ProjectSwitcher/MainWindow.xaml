<Controls:MetroWindow  x:Class="Fluor.ProjectSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Fluor.ProjectSwitcher"          
        xmlns:View="clr-namespace:Fluor.ProjectSwitcher.View"      
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"  
        Title="" SizeToContent="WidthAndHeight" FontSize="16" BorderThickness="1" MaxHeight="900" MinHeight="280" MinWidth="370" DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}" BorderBrush="Black" FontFamily="Calibri" ShowTitleBar="False" ShowMaxRestoreButton="False" ShowMinButton="False" ShowCloseButton="False" ShowSystemMenuOnRightClick="False">
    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout Name="flySettings" Header="settings" Position="Left" Width="Auto" IsOpenChanged="flyOut_IsOpenChanged">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Stroke="White" Height="Auto" Width="1" HorizontalAlignment="Left"/>
                    <StackPanel Grid.Column="1">
                        <MenuItem Name="miViewSettingsFile" Header="View Settings File" Icon="{DynamicResource SettingsIcon}" Click="miViewSettingsFile_Click" Foreground="White" FontSize="12"/>
                        <MenuItem Name="miAdmin" Header="Open Admin Module" Icon="{DynamicResource SettingsIcon}" Foreground="White" FontSize="12" Click="miAdmin_Click"/>
                        <MenuItem Name="miRefresh" Header="Refresh After Changes" Icon="{DynamicResource RefreshIcon}" Foreground="White" FontSize="12" Click="miRefresh_Click"/>
                        <MenuItem Name="miCloseAllApps" Header="Close All Open Apps" Icon="{DynamicResource CloseIcon}" Click="miCloseAllApps_Click" Foreground="White" FontSize="12"/>
                        <MenuItem Name="miAbout" Header="About" Icon="{DynamicResource AboutIcon}" Click="miAbout_Click" Foreground="White" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Controls:Flyout>
            <Controls:Flyout Name="flyAbout" Header="about" Position="Right" Width="Auto" IsOpenChanged="flyOut_IsOpenChanged" MouseDown="flyAbout_MouseDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Rectangle Stroke="White" Height="1" VerticalAlignment="Top"/>
                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label Content="FLUOR" FontSize="22" HorizontalAlignment="Center"/>
                        <Label Content="Project Switcher" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <Label Name="lblVersion" Content="version 1.0" HorizontalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}"/>
                    </StackPanel>
                </Grid>
            </Controls:Flyout>
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <!--<Rectangle Grid.ColumnSpan="4" Fill="{DynamicResource HighlightBrush}" />
        <Label Content="Projects" Grid.Column="1" Margin="20,0,0,0" Foreground="White" />
        <Label Content="Applications" Grid.Column="2" Margin="20,0,0,0" Foreground="White" />-->
        <!--<Rectangle Fill="{DynamicResource HighlightBrush}" Width="Auto"/>-->
        <!--<Button Style="{DynamicResource BackButton}" HorizontalAlignment="Left"/>-->
        <Grid Background="#FF00457D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Name="btnSettings" Height="35" Width="35" VerticalAlignment="Center" Click="btnSettings_Click" Content="{DynamicResource MenuIcon}" BorderBrush="{x:Null}" Style="{DynamicResource MetroCircleButtonStyleCustomSpin}" ToolTip="SETTINGS" Foreground="White"/>
            <Button Name="btnHome" Grid.Column="1" Style="{DynamicResource MetroCircleButtonStyleCustomWobble}" Content="{DynamicResource HomeIcon}" Height="35" Width="35" Click="btnHome_Click" ToolTip="HOME" Foreground="White"/>
            <ItemsControl ItemsSource="{Binding BreadcrumbCollection}" ItemTemplate="{Binding Mode=OneWay, Source={StaticResource MetroBreadCrumbButtonStyle}}" VerticalAlignment="Center" Grid.Column="2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <Button Name="btnEditMode" Height="35" Width="35" VerticalAlignment="Center" Content="{DynamicResource EditIcon}" Click="miAdmin_Click" BorderBrush="{x:Null}" Style="{DynamicResource MetroCircleButtonStyleCustom}" Grid.Column="3" ToolTip="EDIT MODE" Foreground="White"/>
            <Button Name="btnClose" Height="35" Width="35" VerticalAlignment="Center" Content="{DynamicResource CloseIcon}" BorderBrush="{x:Null}" Style="{DynamicResource MetroCircleButtonStyleCustomSpin}" Grid.Column="4" Click="btnClose_Click" ToolTip="CLOSE" Margin="0,0,5,0" Foreground="White"/>
        </Grid>

        <Rectangle Grid.Row="1" Grid.RowSpan="3" Fill="{DynamicResource AccentColorBrush4}" Width="Auto"/>
        <TabControl Grid.Row="1">
            <TabItem Name="tbiTiles" Header="Tiles" IsSelected="{Binding IsTileTabSelected}" Visibility="Collapsed">
                <View:ViewTiles Grid.Row="1" Grid.Column="1" Margin="0,10,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </TabItem>
            <TabItem Name="tbiApplications" Header="Apps" IsSelected="{Binding IsApplicationsTabSelected}" Visibility="Collapsed">
                <View:ViewApplications Grid.Row="1" Grid.Column="2" Margin="0,10,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </TabItem>
            <TabItem Name="tbiAddNew" Header="Add New" IsSelected="{Binding IsAddNewTabSelected}" Visibility="Collapsed">
                <View:ViewAddNew Grid.Row="1" Grid.Column="2" Margin="0,10,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </TabItem>
        </TabControl>
        <!--<Rectangle Grid.Row="1" Grid.Column="1" Width="1" HorizontalAlignment="Right" Stroke="{DynamicResource AccentColorBrush4}"/>-->
        <!--<Label Name="lblPlantName" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="12" Content="{Binding SelectedProject.MiscText}"/>-->
        <Button Name="btnOpenProject" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" Click="btnOpenProject_Click" Width="60" Height="60" ToolTip="SWITCH PROJECT" Content="{DynamicResource SwitchProjectIcon}" Style="{DynamicResource MetroCircleButtonStyleCustomSpin}" Background="{DynamicResource SubColorBrush}" Foreground="White"/>
        <local:StatusWindow x:Name="StatusWindow" Grid.RowSpan="4" Grid.ColumnSpan="4" Visibility="Hidden"/>
        <Rectangle Name="recDisable" Grid.RowSpan="3" Grid.ColumnSpan="4" Opacity="0.5" Visibility="Hidden" MouseDown="recDisable_MouseDown">
            <Rectangle.Fill>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</Controls:MetroWindow>
