<Controls:MetroWindow  x:Class="Fluor.ProjectSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Fluor.ProjectSwitcher"          
        xmlns:View="clr-namespace:Fluor.ProjectSwitcher.View"      
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"  
        Title="SPPID PROJECT SWITCHER" SizeToContent="WidthAndHeight" FontSize="16" BorderThickness="1" MaxHeight="900" MinHeight="380" MinWidth="520" ShowTitleBar="False" DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}" >
    <Controls:MetroWindow.Flyouts>
        <Controls:FlyoutsControl>
            <Controls:Flyout Name="flySettings" Header="settings" Position="Bottom" Width="Auto" IsOpenChanged="flyOut_IsOpenChanged">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Stroke="White" Height="Auto" Width="1" HorizontalAlignment="Left"/>
                    <StackPanel Grid.Column="1">
                        <MenuItem Name="miViewSettingsFile" Header="View Settings File" Icon="{DynamicResource SettingsWhiteIcon}" Click="miViewSettingsFile_Click" Foreground="White" FontSize="12"/>
                        <MenuItem Name="miRefresh" Header="Refresh After Changes" Icon="{DynamicResource RefreshIcon}" Foreground="White" FontSize="12" Click="miRefresh_Click"/>
                        <MenuItem Name="miCloseAllApps" Header="Close All Open Apps" Icon="{DynamicResource CloseIcon}" Click="miCloseAllApps_Click" Foreground="White" FontSize="12"/>
                        <MenuItem Name="miAbout" Header="About" Icon="{DynamicResource AboutIcon}" Click="miAbout_Click" Foreground="White" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Controls:Flyout>
            <Controls:Flyout Name="flyAbout" Header="about" Position="Right" Width="520" IsOpenChanged="flyOut_IsOpenChanged" MouseDown="flyAbout_MouseDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Rectangle Stroke="White" Height="1" VerticalAlignment="Top"/>
                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Label Content="FLUOR" FontSize="22" HorizontalAlignment="Center"/>
                        <Label Content="Project Switcher" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <Label Name="lblVersion" Content="version 1.0" HorizontalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}"/>
                    </StackPanel>
                </Grid>
            </Controls:Flyout>
        </Controls:FlyoutsControl>
    </Controls:MetroWindow.Flyouts>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition MinWidth="250"/>
            <ColumnDefinition MinWidth="250"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="15"/>
            <RowDefinition />
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.ColumnSpan="4" Fill="{DynamicResource HighlightBrush}" />
        <Label Content="Project Switcher" Grid.Column="1" FontSize="20" Foreground="White" />
        <View:ProjectsUC Grid.Row="2" Grid.Column="1"/>
        <Rectangle Grid.Row="2" Grid.Column="1" Width="1" HorizontalAlignment="Right" Stroke="{DynamicResource AccentColorBrush4}"/>
        <View:ApplicationsUC Grid.Row="2" Grid.Column="2"/>
        <Label Name="lblPlantName" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="12" Content="{Binding SelectedProject.MiscText}"/>
        <Button Name="btnOpenProject" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="3" Margin="0,10,0,0" Click="btnOpenProject_Click" Width="50" Height="40" ToolTip="switch project" Content="{DynamicResource SwitchProjectIcon}" />
        <Button Name="btnSettings" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="3" Width="25" Height="25" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="btnSettings_Click" Content="{DynamicResource SettingsIcon}"/>
        <local:StatusWindow x:Name="StatusWindow" Grid.RowSpan="4" Grid.ColumnSpan="4" Visibility="Hidden"/>
        <Rectangle Name="recDisable" Grid.RowSpan="6" Grid.ColumnSpan="4" Opacity="0.5" Visibility="Hidden" MouseDown="recDisable_MouseDown">
            <Rectangle.Fill>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</Controls:MetroWindow>
